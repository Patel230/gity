# Gity — public frontend configuration (safe to commit; no secrets here).
# There is deliberately NO GitHub token in this file.
#
# Hosting is Cloudflare Pages (static export in out/) + Pages Functions
# (functions/api/*) for login, encrypted sessions, and the GitHub relay. No
# build-time secrets are needed:
# GITHUB_CLIENT_ID is public and set in the Cloudflare dashboard, and
# GITHUB_CLIENT_SECRET and SESSION_ENCRYPTION_KEY are encrypted Pages secrets.

# Cloudflare Pages runtime variables/secrets (configure with `wrangler pages
# secret put` or the dashboard; do not put real values in this file):
# GITHUB_CLIENT_ID=Iv1.…
# GITHUB_CLIENT_SECRET=…
# SESSION_ENCRYPTION_KEY=generate-a-long-random-secret

# Read-only agent API (optional). These are configured as Cloudflare Pages
# environment variables/secrets, not exposed to the browser.
# GITY_AGENT_TOKEN=generate-a-long-random-bearer-token
# GITHUB_APP_ID=your-github-app-id
# GITHUB_APP_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----... (encrypted secret)
# GITHUB_INSTALLATION_ID=installation-id-for-the-allowed-account
# GITY_AGENT_ORIGIN=https://your-agent-client.example (optional CORS origin)

# Link to this repo, shown in the footer/sidebar ("Star on GitHub").
NEXT_PUBLIC_REPO_URL=https://github.com/Patel230/gity
